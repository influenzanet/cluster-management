apiVersion: v1
kind: Service
metadata:
  name: user-management-service
  namespace: case
spec:
  type: ClusterIP
  clusterIP: 10.96.0.13
  selector:
    app: user-management
  ports:
  - name: grpc
    protocol: TCP
    port: 3232
    targetPort: 3232